/* eslint-disable prefer-destructuring */

export const API_ROOT = window._env_.API_ROOT;
export const ENABLE_CAPTCHA = window._env_.ENABLE_CAPTCHA;
export const RECAPTCHA_KEY = window._env_.RECAPTCHA_KEY;
